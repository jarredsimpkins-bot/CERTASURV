# CertaSurv Queue Worker Extension

This folder adds the unattended queue-worker layer to the draft `D:\SERVER` router. It executes only verified deterministic capabilities and low-authority Ollama candidate tasks. Codex, specialist-node, and human-review tasks remain queued for their proper execution authority.
